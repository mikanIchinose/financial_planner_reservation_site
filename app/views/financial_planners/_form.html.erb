<%= form_with(model: financial_planner, local: true) do |form| %>
  <% if financial_planner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(financial_planner.errors.count, "error") %> prohibited this financial_planner from being saved:</h2>

      <ul>
        <% financial_planner.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :phone_number %>
    <%= form.text_field :phone_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
